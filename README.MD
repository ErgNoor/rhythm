# Run tests
1. Clone repo: ```$ https://github.com/ErgNoor/rhythm.git```
2. Cd repo: ```$ cd rhythm```
3. Create virtual env: ```$ python -m venv env```
4. Activate virtual env: ```$ source ./env/bin/activate```
5. Install libs: ```$ pip install requirements.txt```
6. Run tests: ```$ pytest -v```
